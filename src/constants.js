const basePath = process.cwd();

const ASSETS_PATH = `${basePath}/assets/`;
const BUILD_PATH = `${basePath}/build/`;
const IMAGES_PATH = `${BUILD_PATH}images/`;
const JSON_PATH = `${BUILD_PATH}json/`;
const LAYERS_PATH = `${ASSETS_PATH}layers/`;

module.exports = {
    ASSETS_PATH,
    BUILD_PATH,
    IMAGES_PATH,
    JSON_PATH,
    LAYERS_PATH
};